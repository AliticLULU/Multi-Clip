<Window x:Class="MemoClip.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MemoClip" Height="200" Width="400"
        ResizeMode="NoResize"
        Background="White"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        WindowStyle="None"
        AllowsTransparency="True"
        BorderBrush="#E2E8F0" BorderThickness="1">
    <Window.Resources>
        <DropShadowEffect x:Key="CardShadow" Color="#20000000" BlurRadius="20" ShadowDepth="4" Direction="270" Opacity="0.5"/>
    </Window.Resources>
    <Border Background="White" CornerRadius="12" Effect="{StaticResource CardShadow}">
        <Grid Margin="24,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0" x:Name="TitleBlock" Text="Confirm"
                       FontSize="16" FontWeight="Bold" Foreground="#1E293B"
                       FontFamily="Segoe UI" Margin="0,0,0,12"/>

            <!-- Message -->
            <TextBlock Grid.Row="1" x:Name="MessageBlock" Text=""
                       FontSize="13.5" Foreground="#475569"
                       FontFamily="Segoe UI" TextWrapping="Wrap"
                       Margin="0,0,0,16"/>

            <!-- Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Width="80" Height="34"
                        Click="Cancel_Click" IsCancel="True"
                        FontSize="13" FontFamily="Segoe UI"
                        Background="White" Foreground="#475569"
                        BorderBrush="#E2E8F0" BorderThickness="1"
                        Cursor="Hand" Margin="0,0,8,0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button x:Name="ActionBtn" Content="Delete" Width="80" Height="34"
                        Click="Action_Click" IsDefault="True"
                        FontSize="13" FontWeight="SemiBold" FontFamily="Segoe UI"
                        Background="#EF4444" Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
